{
  "name": "@enkap/webhook-hub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn src/main.ts",
    "build": "tsc",
    "start": "node dist/main",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@enkap/shared-types": "workspace:*",
    "fastify": "^4.26.0",
    "pg": "^8.11.5",
    "pino-pretty": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.5",
    "typescript": "^5.4.0",
    "ts-node-dev": "^2.0.0"
  }
}
